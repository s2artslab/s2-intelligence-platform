{
  "name": "s2-intelligence-public-api",
  "version": "0.1.0",
  "private": true,
  "description": "S² Intelligence public API — thin server; users supply Groq API keys",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check lib/prompts.js && node --check lib/ollama.js && node --check lib/unified-lora.js && node --check lib/rag.js && node --check lib/canon.js && node --check lib/cadence-router.js && node --check lib/retrieval-index.js && node --check lib/continuity.js && node --check lib/memory/tension-store.js && node --check lib/memory/reflection.js && node --check lib/billing.js",
    "build:retrieval-index": "node scripts/build-retrieval-index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
